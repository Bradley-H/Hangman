// WORDS ARRAY //

let words: string[] = [
  "applications",
  "programmer",
  "anime",
  "wizard",
  "master",
];

let selectedWord = words[Math.floor(Math.random() * words.length)];

let correctLetters: any = [];

let wrongLetters: any = [];
